<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="eBRestarter.Desktop.WinUI3.VisualUIComponents.Views.Dialogs.DeleteBrowserContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:convertersNuget="using:CommunityToolkit.WinUI.Converters"
    xmlns:VM="using:eBRestarter.Desktop.WinUI3.ViewModels"
    d:DataContext ="{d:DesignInstance Type=VM:ViewModelDeleteBrowserContent}"
    mc:Ignorable="d"
    x:Uid="DeleteDialog"
    DefaultButton="Close"
    Style="{StaticResource DefaultContentDialogStyle}"
    RequestedTheme="Dark">

    <ContentDialog.Resources>
        <convertersNuget:BoolNegationConverter x:Key="BoolNegationConverter"/>
        <convertersNuget:BoolToVisibilityConverter x:Key="BoolToVisConverter"/>

        <convertersNuget:BoolToVisibilityConverter x:Key="InverseBoolToVisConverter"
                                                   TrueValue="Collapsed"
                                                   FalseValue="Visible"/>

        <Style TargetType="TextBlock" x:Key="HeaderTextStyle">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
        </Style>
    </ContentDialog.Resources>

    <Grid MinWidth="500">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,25">
            <Image Source="{Binding BrowserIconPath, Mode=OneWay}"
                   Width="32" Height="32"
                   VerticalAlignment="Center"/>

            <TextBlock Text="{Binding BrowserName, Mode=OneWay}"
                       FontSize="20"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="15,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,25">

            <TextBlock x:Uid="DeleteDialog_HeaderOptions" 
                       Style="{StaticResource HeaderTextStyle}"/>

            <CheckBox x:Uid="DeleteDialog_CheckCookies"
                      IsChecked="{Binding IsDeleteCookiesChecked, Mode=TwoWay}"
                      IsEnabled="{Binding IsBusy, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}"
                      Margin="0,0,0,10"/>

            <CheckBox x:Uid="DeleteDialog_CheckCache"
                      IsChecked="{Binding IsDeleteInternetCacheChecked, Mode=TwoWay}"
                      IsEnabled="{Binding IsBusy, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}"/>

        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,25">

            <TextBlock x:Uid="DeleteDialog_HeaderProgress"
                       Style="{StaticResource HeaderTextStyle}"/>

            <Grid Margin="0,0,0,5">

                <ProgressBar Value="{Binding ProgressValue, Mode=OneWay}"
                             Maximum="{Binding ProgressMaximum, Mode=OneWay}"
                             Height="10"
                             IsEnabled="{Binding IsBusy, Mode=OneWay}"/>
            </Grid>

            <Grid>

                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>

                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding StatusText, Mode=OneWay}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           FontSize="12"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis"
                           ToolTipService.ToolTip="{Binding StatusText, Mode=OneWay}"
                           VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1"
                           Text="{Binding ProgressText, Mode=OneWay}"
                           FontWeight="Bold"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,10,0,0">

            <Button x:Uid="DeleteDialog_BtnStart"
                    Command="{Binding StartCleaningCommand}"
                    Visibility="{Binding IsBusy, Converter={StaticResource InverseBoolToVisConverter}, Mode=OneWay}"
                    HorizontalAlignment="Stretch"
                    Background="{ThemeResource SystemFillColorSuccessBrush}"
                    Foreground="White"/>

            <Button x:Uid="DeleteDialog_BtnCancel"
                    Command="{Binding CancelCleaningCommand}"
                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}, Mode=OneWay}"
                    HorizontalAlignment="Stretch"
                    Background="{ThemeResource SystemFillColorCriticalBrush}"
                    Foreground="White"/>
        </Grid>

        <Grid Grid.Row="0" Grid.RowSpan="4" 
              Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" 
              CornerRadius="8"
              Padding="20"
              Visibility="{Binding IsProcessConflict, Converter={StaticResource BoolToVisConverter}, Mode=OneWay}">

            <StackPanel VerticalAlignment="Center" Spacing="15">
                <FontIcon Glyph="&#xE7BA;" FontSize="40" Foreground="{ThemeResource SystemFillColorCriticalBrush}" HorizontalAlignment="Center"/>

                <TextBlock x:Uid="ConflictDialog_Title"
                           FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>

                <TextBlock x:Uid="DeleteDialog_ConflictMessage"
                           TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" Opacity="0.8"/>

                <Grid Margin="0,15,0,0" ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            x:Uid="DeleteDialog_BtnForceClose"
                            Command="{Binding ForceCloseAndContinueCommand}"
                            HorizontalAlignment="Stretch"
                            Style="{StaticResource AccentButtonStyle}"/>

                    <Button Grid.Column="1" 
                            x:Uid="DeleteDialog_BtnAbort"
                            Command="{Binding CancelConflictCommand}"
                            HorizontalAlignment="Stretch"/>
                </Grid>
            </StackPanel>
        </Grid>

    </Grid>
</ContentDialog>