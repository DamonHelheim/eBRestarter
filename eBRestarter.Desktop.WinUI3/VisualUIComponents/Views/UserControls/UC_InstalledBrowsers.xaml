<UserControl
    x:Class="eBRestarter.Desktop.WinUI3.VisualUIComponents.Views.UserControls.UC_InstalledBrowsers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:userControls="using:eBRestarter.Desktop.WinUI3.VisualUIComponents.Views.UserControls"
    mc:Ignorable="d">

    <Grid
        Padding="4"
        Background="{ThemeResource DashletBackgroundColor_1}"
        BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="8">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">

            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />

            </Grid.ColumnDefinitions>

            <Image
                x:Name="image_installed_browsers"
                Width="24"
                Height="24"
                Margin="0,0,12,0"
                VerticalAlignment="Center"
                Source="{ThemeResource GlobeIcon}" />

            <TextBlock
                x:Name="tbl_Installed_Browsers"
                x:Uid="Installed_browser"
                Grid.Column="1"
                VerticalAlignment="Center"
                FontSize="18"
                Foreground="{ThemeResource FontColor_1}"
                Text="Installierte Browser" />

        </Grid>

        <ScrollViewer
            Grid.Row="1"
            HorizontalAlignment="Left"
            BorderThickness="0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">

            <ItemsControl ItemsSource="{Binding Browsers}">

                <ItemsControl.ItemTemplate>

                    <DataTemplate>
                        <userControls:UC_Browser DataContext="{Binding}" />
                    </DataTemplate>

                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Spacing="4" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

        </ScrollViewer>
    </Grid>
</UserControl>